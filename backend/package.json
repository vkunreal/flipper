{
  "name": "flipper-backend",
  "version": "1.0.0",
  "description": "backend",
  "main": "app.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "dev": "npm run build && concurrently \"npm run build:watch\" \"npm run serve:watch\"",
    "serve": "node dist/app.js",
    "serve:watch": "nodemon dist/app.js"
  },
  "author": "vkunreal",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.17",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
